# 🌐 Настройка Local Bot API Server

Local Bot API Server позволяет обрабатывать файлы размером до **2000 MB** вместо стандартных 20 MB.

## 📋 Требования

- Docker и Docker Compose
- API ID и API Hash от Telegram

## 🔑 Получение API_ID и API_HASH

### Шаг 1: Перейдите на https://my.telegram.org

1. Откройте https://my.telegram.org в браузере
2. Войдите используя ваш номер телефона
3. Введите код подтверждения из Telegram

### Шаг 2: Создайте приложение

1. Перейдите в раздел **"API development tools"**
2. Заполните форму создания приложения:
   - **App title**: MyAudioBot (или любое другое имя)
   - **Short name**: myaudiobot (только латиница, маленькие буквы)
   - **Platform**: Other
   - **Description**: Audio processing bot (опционально)

3. Нажмите **"Create application"**

### Шаг 3: Скопируйте данные

После создания вы увидите:
- **App api_id**: `12345678` (ваш API ID)
- **App api_hash**: `abcdef1234567890abcdef1234567890` (ваш API Hash)

**⚠️ Важно:** Эти данные конфиденциальны! Не публикуйте их в открытом доступе.

## ⚙️ Настройка бота

### 1. Обновите файл `.env`

```bash
# Скопируйте .env.example в .env если еще не создан
cp .env.example .env
```

Откройте `.env` и добавьте ваши данные:

```env
# Telegram Bot Token (от @BotFather)
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# API ID и API Hash (от https://my.telegram.org)
TELEGRAM_API_ID=12345678
TELEGRAM_API_HASH=abcdef1234567890abcdef1234567890

# Включить Local Bot API Server
USE_LOCAL_API=true

# URL локального API (не меняйте)
LOCAL_API_URL=http://telegram-bot-api:8081

# Максимальный размер файла (до 2000 MB)
MAX_FILE_SIZE_MB=2000
```

### 2. Запустите бота

```bash
# Пересоберите и запустите контейнеры
docker-compose down
docker-compose up -d --build

# Проверьте логи
docker-compose logs -f telegram-audio-bot
```

Вы должны увидеть в логах:
```
🚀 Telegram Audio Bot PRO v2.5
✨ Версия: 2.5 (Local Bot API)
🌐 Local API: ✅ Включен
🔗 API URL: http://telegram-bot-api:8081
📦 Макс. размер файла: 2000 МБ
```

## 🎯 Проверка работы

1. Отправьте боту аудиофайл размером больше 20 MB
2. Бот должен успешно обработать файл
3. Файлы до 2000 MB теперь поддерживаются

## 🔧 Отключение Local API

Если хотите вернуться к стандартному API (лимит 20 MB):

1. Откройте `.env`
2. Измените: `USE_LOCAL_API=false`
3. Перезапустите: `docker-compose restart telegram-audio-bot`

## ❓ Проблемы и решения

### Ошибка: "connection refused" или "timeout"

**Проблема**: Bot API Server не запустился

**Решение**:
```bash
# Проверьте статус контейнера
docker-compose ps

# Проверьте логи Bot API Server
docker-compose logs telegram-bot-api

# Убедитесь что API_ID и API_HASH правильные
docker-compose down
docker-compose up -d
```

### Ошибка: "Unauthorized"

**Проблема**: Неправильный API_ID или API_HASH

**Решение**:
1. Проверьте данные на https://my.telegram.org
2. Обновите `.env` с правильными значениями
3. Перезапустите: `docker-compose restart`

### Файлы все еще ограничены 20 MB

**Проблема**: Local API не включен или не работает

**Решение**:
```bash
# Проверьте логи бота
docker-compose logs telegram-audio-bot

# Должно быть: "🌐 Local API: ✅ Включен"
# Если нет, проверьте USE_LOCAL_API=true в .env
```

## 📊 Производительность

- **CPU**: +20-30% нагрузки при обработке больших файлов
- **RAM**: +500MB-1GB для Bot API Server
- **Диск**: Bot API Server кеширует данные (~100-500 MB)

## 🔒 Безопасность

- **API_ID** и **API_HASH** хранятся только в `.env` файле
- Не коммитьте `.env` в Git (уже в `.gitignore`)
- Bot API Server работает только внутри Docker сети
- Порт 8081 доступен только для бота (не для внешних подключений)

## 📚 Дополнительная информация

- [Telegram Bot API Documentation](https://core.telegram.org/bots/api)
- [telegram-bot-api GitHub](https://github.com/tdlib/telegram-bot-api)
- [Docker Image: aiogram/telegram-bot-api](https://hub.docker.com/r/aiogram/telegram-bot-api)

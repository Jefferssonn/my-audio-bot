# 🎵 Telegram Audio Bot PRO v2.6

Профессиональный Telegram-бот для обработки аудио с мягкой компрессией и сохранением динамического диапазона.

## ✨ Возможности

- **📊 Детальный анализ** - анализ качества, динамики и частотных характеристик
- **📈 Спектральный анализ** - визуализация формы волны и частотного спектра
- **✨ Мягкое улучшение** - три уровня компрессии (1.5:1, 2.0:1, 3.0:1) с сохранением динамики
- **🔊 Нормализация громкости** - точная нормализация по стандарту LUFS (-16 LUFS)
- **🎵 Конвертация Моно → Стерео** - преобразование моно в стерео
- **💾 Конвертация форматов** - поддержка FLAC, MP3 (320kbps), OGG Vorbis (q10), WAV
- **🚀 Полная обработка** - автоматическая обработка со всеми улучшениями

## 🎚️ Особенности v2.6

- ✅ **Local Bot API Server** - поддержка файлов до **2000 MB** (вместо 20 MB) 🔥
- ✅ **Безопасная загрузка** - проверка размера файлов с учетом лимитов API
- ✅ **Выбор формата** - FLAC/MP3/OGG/WAV для всех операций
- ✅ **Мягкая компрессия** - коэффициенты от 1.5:1 до 3.0:1 вместо агрессивных 4-6:1
- ✅ **Сохранение динамики** - естественное звучание без "сдавливания"
- ✅ **Правильная громкость** - точная нормализация по LUFS
- ✅ **Без потери качества** - экспорт в lossless FLAC (по умолчанию)
- ✅ **Headroom** - оставляет 3dB запаса перед clipping

## 📋 Требования

- [Docker](https://www.docker.com/get-started) и Docker Compose
- Telegram Bot Token (получите у [@BotFather](https://t.me/BotFather))

## 🚀 Быстрый старт

### 1. Клонирование проекта

Если вы еще не клонировали проект, создайте директорию:

```bash
mkdir MyAudioBot
cd MyAudioBot
```

### 2. Получение токена бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и придумайте имя бота
4. Скопируйте полученный токен (например: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 3. Настройка переменных окружения

Скопируйте файл `.env.example` в `.env`:

```bash
copy .env.example .env
```

Откройте `.env` и замените `YOUR_BOT_TOKEN_HERE` на ваш реальный токен:

```env
BOT_TOKEN=ваш_токен_здесь
MAX_FILE_SIZE_MB=100
CLEANUP_INTERVAL_MINUTES=30
TEMP_FILE_MAX_AGE_HOURS=2
```

#### 🌐 Опционально: Local Bot API для больших файлов (до 2000 MB)

Если нужно обрабатывать файлы больше 20 MB, настройте Local Bot API:

1. Получите API_ID и API_HASH на https://my.telegram.org
2. Добавьте в `.env`:
```env
TELEGRAM_API_ID=12345678
TELEGRAM_API_HASH=your_api_hash_here
USE_LOCAL_API=true
MAX_FILE_SIZE_MB=2000
```

📚 **Подробная инструкция**: [LOCAL_API_SETUP.md](LOCAL_API_SETUP.md)

### 4. Запуск бота

#### Windows (PowerShell):

```powershell
# Запуск бота
.\scripts\start.bat

# Остановка бота
.\scripts\stop.bat

# Просмотр логов
.\scripts\logs.bat

# Перезапуск бота
.\scripts\restart.bat
```

#### Linux/Mac:

```bash
# Запуск бота
./scripts/start.sh

# Остановка бота
./scripts/stop.sh

# Просмотр логов
./scripts/logs.sh

# Перезапуск бота
./scripts/restart.sh
```

Или используйте Docker Compose напрямую:

```bash
docker-compose up -d
```

## 📖 Использование

### Команды бота

1. Отправьте команду `/start` боту в Telegram
2. Отправьте аудиофайл (поддерживаются форматы: MP3, WAV, FLAC, OGG, M4A и др.)
3. Выберите нужное действие из меню:

#### 📊 Анализ
Получите детальную информацию о файле:
- Каналы (моно/стерео)
- Частота дискретизации
- Битность
- Длительность и размер
- Качество (%)
- RMS, Peak, динамический диапазон
- Громкость в LUFS

#### 📈 Спектр
Визуализация аудио:
- Форма волны (waveform)
- Частотный спектр (FFT)

#### ✨ Улучшение
Три уровня компрессии:
- **Light (1.5:1)** - самая мягкая, для музыки с высокой динамикой
- **Medium (2.0:1)** - рекомендуется для большинства случаев
- **Heavy (3.0:1)** - для подкастов и голоса

#### 🔊 Нормализация
Нормализация громкости до -16 LUFS (стандарт для стриминговых сервисов)

#### 🎵 Моно → Стерео
Конвертация моно-записи в стерео

#### 💾 Конвертер
Поддерживаемые форматы:
- **FLAC** - без потерь (lossless)
- **MP3** - 320 kbps (высокое качество)
- **OGG Vorbis** - q10 (высокое качество)
- **WAV** - PCM без сжатия

#### 🚀 Полная обработка
Автоматически применяет:
1. Конвертацию моно → стерео (если нужно)
2. Мягкую компрессию (Medium 2:1)
3. Нормализацию громкости
4. Экспорт в FLAC
5. Графики сравнения и спектра (для файлов до 2 мин)

## ⚙️ Настройка

### Переменные окружения (.env)

| Переменная | Описание | Значение по умолчанию |
|------------|----------|----------------------|
| `BOT_TOKEN` | Токен Telegram бота | обязательно |
| `MAX_FILE_SIZE_MB` | Максимальный размер файла (МБ) | 100 |
| `CLEANUP_INTERVAL_MINUTES` | Интервал очистки temp (мин) | 30 |
| `TEMP_FILE_MAX_AGE_HOURS` | Максимальный возраст temp файлов (час) | 2 |

### Ресурсы Docker

В `docker-compose.yml` настроены лимиты:

```yaml
deploy:
  resources:
    limits:
      memory: 2G      # Максимум памяти
      cpus: '2'       # Максимум CPU
    reservations:
      memory: 512M    # Гарантированная память
```

## 📊 Мониторинг

### Просмотр логов

```bash
# Последние 100 строк
docker-compose logs --tail=100

# Мониторинг в реальном времени
docker-compose logs -f

# Логи внутри контейнера
docker exec telegram-audio-bot-pro cat /app/logs/bot.log
```

### Статус контейнера

```bash
docker-compose ps
```

### Health check

```bash
docker inspect telegram-audio-bot-pro --format='{{.State.Health.Status}}'
```

## 🛠️ Управление

### Остановка бота

```bash
docker-compose down
```

### Перезапуск после изменений

```bash
docker-compose restart
```

### Полная переустановка

```bash
docker-compose down -v  # Удаляет volumes
docker-compose up -d
```

### Очистка временных файлов

Временные файлы автоматически очищаются каждые 30 минут.
Для ручной очистки:

```bash
docker exec telegram-audio-bot-pro rm -rf /app/temp/*
```

## 📁 Структура проекта

```
MyAudioBot/
├── docker-compose.yml    # Конфигурация Docker
├── .env                  # Переменные окружения (не коммитить!)
├── .env.example          # Шаблон переменных окружения
├── README.md             # Документация
├── .gitignore            # Игнорируемые файлы для Git
├── logs/                 # Логи бота (volume)
├── temp/                 # Временные файлы (volume)
└── scripts/              # Скрипты управления
    ├── start.bat         # Запуск (Windows)
    ├── stop.bat          # Остановка (Windows)
    ├── restart.bat       # Перезапуск (Windows)
    ├── logs.bat          # Логи (Windows)
    ├── start.sh          # Запуск (Linux/Mac)
    ├── stop.sh           # Остановка (Linux/Mac)
    ├── restart.sh        # Перезапуск (Linux/Mac)
    └── logs.sh           # Логи (Linux/Mac)
```

## 🔒 Безопасность

- ⚠️ **Никогда не коммитьте** файл `.env` с токеном в Git
- ✅ Используйте `.env.example` как шаблон
- ✅ Добавьте `.env` в `.gitignore`
- ✅ Rate limiting: 5 запросов в минуту на пользователя
- ✅ Автоочистка временных файлов

## 🐛 Устранение проблем

### Бот не запускается

1. Проверьте, что Docker запущен
2. Проверьте токен в `.env`
3. Посмотрите логи: `docker-compose logs`

### Бот не отвечает

1. Проверьте статус: `docker-compose ps`
2. Проверьте логи: `docker-compose logs -f`
3. Перезапустите: `docker-compose restart`

### Ошибки обработки аудио

- Проверьте формат файла (поддерживаются: MP3, WAV, FLAC, OGG, M4A)
- Проверьте размер файла (лимит: 100 МБ по умолчанию)
- Для файлов > 5 минут используйте отдельные функции вместо "Полной обработки"

### Недостаточно памяти

Увеличьте лимит памяти в `docker-compose.yml`:

```yaml
limits:
  memory: 4G  # Увеличить до 4GB
```

## 📝 Лицензия

MIT License

## 🤝 Поддержка

Если у вас возникли проблемы или вопросы:

1. Проверьте раздел "Устранение проблем"
2. Посмотрите логи бота
3. Создайте Issue в репозитории

## 🔄 Обновления

### v2.6 (текущая)
- 🌐 Local Bot API Server - поддержка файлов до **2000 MB**
- 🚀 Автоматическое переключение между стандартным и локальным API
- 📦 Настройка через docker-compose с одной командой
- 📚 Подробная документация по настройке ([LOCAL_API_SETUP.md](LOCAL_API_SETUP.md))

### v2.4
- 🛡️ Проверка размера файла перед скачиванием
- 📏 Информативные сообщения об ограничениях Telegram API (20 MB)
- 🔧 Улучшенная обработка ошибок при загрузке файлов
- ✨ Подсказки для пользователей при превышении лимита

### v2.3
- 💾 Выбор формата сохранения (FLAC/MP3/OGG/WAV)
- 🎯 Выбор формата для всех операций
- 📝 Детальные описания форматов
- ⭐ FLAC рекомендуется по умолчанию

### v2.2
- ✨ Улучшенный UI с персонализированным приветствием
- 🎯 Удобная навигация с кнопками возврата
- 📊 Расширенная статистика с TOP-5 операций
- 🎨 Красивые подменю для компрессии и форматов
- 📚 Детальная справка по всем функциям

### v2.1
- Добавлена полная обработка
- Улучшена визуализация

### v2.0
- Добавлена компрессия
- Добавлены графики
- Rate limiting

---

**Сделано с ❤️ для любителей качественного звука**

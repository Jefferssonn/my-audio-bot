# 🚀 Быстрый старт за 5 минут

## 1️⃣ Получите токен бота (2 минуты)

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте `/newbot`
4. Придумайте имя бота
5. **Скопируйте токен** (например: `123456789:ABCdefGHI...`)

## 2️⃣ Настройте проект (1 минута)

### Windows:

```cmd
# 1. Откройте .env файл в блокноте
notepad .env

# 2. Замените YOUR_BOT_TOKEN на ваш токен
# BOT_TOKEN=123456789:ABCdefGHI...
```

### Linux/Mac:

```bash
# 1. Откройте .env в редакторе
nano .env

# 2. Замените YOUR_BOT_TOKEN на ваш токен
# BOT_TOKEN=123456789:ABCdefGHI...
```

## 3️⃣ Запустите бота (1 минута)

### Windows:

Просто запустите файл двойным кликом:

```
scripts\start.bat
```

Или из командной строки:

```cmd
cd e:\ProgrammingFiles\MyAudioBot
scripts\start.bat
```

### Linux/Mac:

```bash
cd /path/to/MyAudioBot

# Сделайте скрипт исполняемым (один раз)
chmod +x scripts/*.sh

# Запустите
./scripts/start.sh
```

### Альтернатива - Docker Compose:

```bash
docker-compose up -d
```

## 4️⃣ Проверьте работу (30 секунд)

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Отправьте любой аудиофайл
4. Выберите действие из меню

## ✅ Готово!

Теперь ваш бот работает 24/7 в фоновом режиме.

## 📊 Полезные команды

### Просмотр логов:

**Windows:** `scripts\logs.bat`
**Linux/Mac:** `./scripts/logs.sh`

### Остановка бота:

**Windows:** `scripts\stop.bat`
**Linux/Mac:** `./scripts/stop.sh`

### Перезапуск:

**Windows:** `scripts\restart.bat`
**Linux/Mac:** `./scripts/restart.sh`

## ⚠️ Проблемы?

### Бот не запускается:

1. Проверьте, что **Docker Desktop запущен** (Windows/Mac)
2. Проверьте, что токен правильно указан в `.env`
3. Убедитесь, что порты не заняты

### Бот не отвечает:

1. Проверьте логи: `scripts\logs.bat` или `./scripts/logs.sh`
2. Перезапустите: `scripts\restart.bat` или `./scripts/restart.sh`

### "Docker not found":

Установите Docker:
- **Windows/Mac:** [Docker Desktop](https://www.docker.com/products/docker-desktop)
- **Linux:** `sudo apt install docker.io docker-compose` или `sudo yum install docker docker-compose`

## 📖 Подробная документация

Смотрите [README.md](README.md) для полной документации и всех возможностей бота.

---

**🎵 Наслаждайтесь качественным звуком!**
